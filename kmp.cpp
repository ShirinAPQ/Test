#include <iostream>
#include <string>
#include <vector>
using namespace std;

//前缀函数
vector<int> next1(const string &p){
    int m = p.size();
    vector<int> next(m,0);
    int len = 0,i = 1;
    while(i < m){
        
    }
    
}
//kmp


int main() {
    string text,pattern;
    cout<<"请输入文本串：";
    cin>>text;
    cout<<"请输入模式串：";
    cin>>pattern;
    //kmp(text,pattern);

    return 0;
}